import 'package:flutter/material.dart';


class Featured extends StatelessWidget {

  Featured({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Choses à faire")
    );
  }

}